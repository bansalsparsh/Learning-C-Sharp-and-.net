#pragma warning disable
using System;
class Calculator
{
    // Generic Method
    public T Calculate<T>(T a, T b)
    {
        // the function should perform the task which can be applied to any data type
        return a;
    }
}